# Active Context: GenericQueue

## Current Work Focus
The primary goal is to provide a "web interface" for the existing GenericQueue application.

## Recent Changes
- Initialized Memory Bank documentation (`projectbrief.md`, `productContext.md`, `systemPatterns.md`, `techContext.md`).
- Analyzed codebase to understand the dynamic XML-driven UI logic.
- Analyzed database schema (partial research of `db_script.sql`).

## Next Steps
1. Propose an implementation plan for the web interface.
2. Determine the technology stack for the web interface (likely Node.js/Vite or Next.js as per system instructions).
3. Create a backend API (or bridge) to provide data from SQL Server to the web frontend.
4. Replicate the dynamic form generation logic in the web frontend.

## Active Decisions
- Use modern web technologies for the new interface while preserving existing database logic.
- Potential stack: Next.js or Vite (HTML/JS/Vanilla CSS).
